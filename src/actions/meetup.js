import { ADD_RSVP, UPDATE_TOPICS } from './types'

export const addRsvp = () => {
  return {
    type: ADD_RSVP
  }
}

export const updateTopics = () => {
 return {
   type: UPDATE_TOPICS
 }
}

//check later if this is right or not 
